import { PlateTypeByIdPipe } from './plateTypeById.pipe';

describe('PlateTypeByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new PlateTypeByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
