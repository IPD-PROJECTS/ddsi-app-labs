import { CheckStatusPlateItemPipe } from './checkStatusPlateItem.pipe';

describe('CheckStatusPlateItemPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckStatusPlateItemPipe();
    expect(pipe).toBeTruthy();
  });
});
