import { FindPlateItemByPositionPipe } from './findPlateItemByPosition.pipe';

describe('FindPlateItemByPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new FindPlateItemByPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
