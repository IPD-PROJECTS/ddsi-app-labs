import { GetListPlatePositionPipe } from './getListPlatePosition.pipe';

describe('GetListPlatePositionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetListPlatePositionPipe();
    expect(pipe).toBeTruthy();
  });
});
