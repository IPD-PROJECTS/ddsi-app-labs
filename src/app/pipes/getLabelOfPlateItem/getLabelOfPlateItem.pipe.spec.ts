import { GetLabelOfPlateItemPipe } from "./getLabelOfPlateItem.pipe";

describe('GetLabelOfPlateItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLabelOfPlateItemPipe();
    expect(pipe).toBeTruthy();
  });
});
