import { NextCaracterPipe } from './nextCaracter.pipe';

describe('NextCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new NextCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
