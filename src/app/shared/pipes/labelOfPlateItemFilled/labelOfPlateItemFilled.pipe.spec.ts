import { LabelOfPlateItemFilledPipe } from './labelOfPlateItemFilled.pipe';

describe('LabelOfPlateItemFilledPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelOfPlateItemFilledPipe();
    expect(pipe).toBeTruthy();
  });
});
